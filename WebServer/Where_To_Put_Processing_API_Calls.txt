The following code locations require API calls to the (pre)processor node(s):
 - ResumeHandler.cs, line 67: A resume was just successfully uploaded to the DB. Send the preprocessor the resume's ID and tell it to fetch the resume from the DB.